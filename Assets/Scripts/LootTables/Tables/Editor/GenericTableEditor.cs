using LootTabler.Entry;
using LootTabler.Table;
using UnityEditor;

[CustomEditor(typeof(GenericTable))]
public class GenericTableEditor : TableEditorBase<TableEntry>
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
